[project]
name = "agentkaggle-leaderboard"
version = "0.1.0"
description = "Aggregate selected Kaggle teams across public competition leaderboards"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kaggle==2.2.3",
    "kagglesdk==0.1.35",
    "python-dotenv==1.2.2",
]
